# Task 03: config and lifecycle

Status: `DONE`

Assignee: `/root`

Depends on: `02.task_repository_bootstrap.md`

## Goal

Start, validate, reload safe settings, drain, and stop predictably.

## Work

- [x] Define versioned YAML schema.
- [x] Implement precedence: defaults, YAML, environment, flags, managed overlay.
- [x] Use secret references. Never return secret values.
- [x] Add `config init` and `config validate` commands.
- [x] Implement signal handling and bounded graceful shutdown.
- [x] Add liveness and readiness state model.
- [x] Define data, config, log, and temporary directory rules.

## Tests

- [x] Invalid config fails before listener opens.
- [x] Environment overrides work without logging values.
- [x] Shutdown completes within deadline and returns nonzero on forced abort.

## Done when

- [x] Lifecycle state transitions are unit-tested.
- [x] Example config contains no plaintext production secret.
- [x] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR: working tree implementation

Commands: `go test ./...`; `go vet ./...`; `accelerator config init`; `accelerator config validate`.

# Task 03: upstream connector

Status: `DONE`

Assignee: `/root`

Depends on: `V0.0.1/03.task_config_and_lifecycle.md`

## Goal

Open, verify, and close one upstream MySQL or MariaDB connection safely.

## Work

- [x] Implement upstream address, TLS, credential, database, and timeout configuration.
- [x] Validate server identity and version metadata.
- [x] Run health probe without changing user data.
- [x] Capture baseline session defaults.
- [x] Classify network, authentication, timeout, and server errors.
- [x] Never log upstream password or full DSN.

## Tests

- [x] Successful MySQL and MariaDB connection tests.
- [x] Bad password, bad certificate, timeout, and unavailable-server tests.
- [x] Reconnect does not reuse corrupt state.

## Done when

- [x] `doctor` can report actionable upstream status.
- [x] Connector errors have stable internal categories.
- [x] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR:

Servers: Laragon MariaDB `11.7.2-MariaDB-log` at `127.0.0.1:3307` and disposable Oracle MySQL `8.4.10` at `127.0.0.1:13308`. Passwordless root access was explicitly opted in only for the local MariaDB configuration; the MySQL lane used a disposable test password. Environment-gated probes and bad-password tests are in `internal/upstream/connector_integration_test.go`.

Automated evidence:

- Unit coverage for TLS policy construction, invalid CA, vendor detection, error categories, fresh bounded handles, unavailable server, earlier context deadline, and credential/DSN redaction.
- `accelerator doctor` emits structured server/session metadata or a categorized actionable error.
- Driver pinned to Go-1.23-compatible `github.com/go-sql-driver/mysql v1.9.3`; source kept unchanged.
- Live `doctor` probe passed and captured `utf8mb4`, `REPEATABLE-READ`, autocommit, SQL mode, collation, and server identity in 12.059 ms without changing data.
- Live `TestIntegrationProbe` and `TestIntegrationBadPassword` passed against the Laragon server.
- The live tests passed again on 2026-07-19. `TestOpenReturnsFreshBoundedHandles` proves reconnect opens independent bounded handles; invalid CA, deadline/timeout classification, and a dynamically reserved unavailable port are covered in the unit lane.
- MySQL 8.4.10 and MariaDB 11.7.2 both passed native probe, bad-password classification, pooled-gateway transaction, datatype, large-row, and 64-client fan-in lanes on 2026-07-19.

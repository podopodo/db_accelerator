# Task 06: differential protocol gate

Status: `DONE`

Assignee: `/root`

Depends on: `04.task_query_result_relay.md`, `05.task_tls_authentication.md`

## Goal

Automatically detect proxy behavior that differs from direct MySQL behavior.

## Work

- [x] Build runner that sends same operation direct and through accelerator.
- [x] Canonicalize only nondeterministic fields explicitly approved by contract.
- [x] Compare metadata, row bytes, status, warnings, errors, affected rows, and insert ID.
- [x] Cover text, Unicode, decimal, date, time, JSON, blob, null, and boundary values.
- [x] Store minimal reproduction on mismatch.
- [x] Add server and driver dimensions from baseline matrix.

## Tests

- [x] Harness detects an intentionally changed status flag.
- [x] Harness detects an intentionally changed error code.
- [x] Clean supported corpus passes repeatedly.

## Done when

- [x] Gate runs in CI integration lane.
- [x] Every mismatch is fixed or added to published limitation list.
- [x] V0.0.2 README exit boxes checked.
- [x] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR: local security and compatibility checkpoint

Report: The seed-21188 datatype and error corpus passed three repeated direct-versus-pooled runs on MariaDB 11.7.2 and a clean run on Oracle MySQL 8.4.10 with `go-sql-driver/mysql` v1.9.3. The first run caught and led to fixes for MariaDB text/JSON metadata, prefix-form unsigned BIGINT metadata, and mismatched temporal client settings. The harness compares column metadata, exact cell bytes, semantic session status, warnings, errors, affected rows, and insert ID, and writes a mode-0600 JSON reproduction only on mismatch. `.github/workflows/compatibility.yml` runs the gate for both server families and retains the test log. Full unit, vet, coverage, build, and race suites passed locally on 2026-07-19.

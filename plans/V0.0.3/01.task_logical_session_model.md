# Task 01: logical session model

Status: `DONE`

Assignee: `/root`

Depends on: `V0.0.2/06.task_differential_protocol_gate.md`

## Goal

Represent all client state needed for safe reuse or pinning.

## Work

- [x] Model selected database, autocommit, transaction state, isolation, read-only mode, charset, collation, time zone, and SQL mode.
- [x] Track warnings, last insert ID, prepared handles, user variables, temporary objects, and lock risk.
- [x] Define state transitions for every supported command.
- [x] Define `multiplexable`, `replayable`, `pinned`, and `poisoned` states.
- [x] Expose pin reason without exposing SQL values.
- [x] Make illegal state transitions fail closed.

## Tests

- [x] Table-driven state transition tests.
- [x] Random command sequence model test.
- [x] Disconnect clears local state.

## Done when

- [x] Every supported command has documented state effect.
- [x] Unknown effect forces pin or rejection.
- [x] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR: next session-correctness checkpoint

Model: `internal/session/logical.go` is integrated into `internal/gateway/gateway.go`. `docs/SESSION_CONTRACT.md` records state effects and reuse decisions without SQL values. Table-driven transitions, 5,000 deterministic random commands, illegal transitions, prepared-handle isolation, and disconnect reset pass in `internal/session`; the live MariaDB pooled integration remains green.

# Task 05: stateful and unsupported commands

Status: `DONE`

Assignee: `/root`

Depends on: `01.task_logical_session_model.md`

## Goal

Handle difficult commands conservatively and visibly.

## Work

- [x] Build command and SQL feature support table.
- [x] Pin temporary tables, locks, user variables, streaming cursors, and non-replayable settings.
- [x] Define behavior for multi-statements, stored routines, local infile, replication commands, admin commands, and unknown packets.
- [x] Add stable unsupported-feature errors where forwarding is unsafe.
- [x] Record pin and rejection reason metrics.
- [x] Publish limitations without hiding them behind generic errors.

## Tests

- [x] Each unsupported feature has explicit test.
- [x] Unknown packet cannot desynchronize session.
- [x] Pin is released only after safe reset.

## Done when

- [x] No unsupported feature silently enters multiplexed path.
- [x] Support table matches code behavior.
- [x] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR: next session-correctness checkpoint

Support table: `docs/SQL_SUPPORT.md` mirrors the classifier and packet dispatch. Table-driven tests cover multi-statements, routines, local infile, file output, locks, SQL-level prepare/execute, handler, replication/reset, maintenance/admin commands, user variables, temporary objects, connection-local functions, CTEs, and unknown statement types. Unknown packet commands receive a stable error without forwarding bytes and the loop remains packet-aligned. Prepared cursors are explicitly refused. The status API and GUI diagnostics expose stable category-only `rejection_reasons`, `pin_reasons`, and reset-discard counts; tests prove snapshots cannot mutate counters. Non-replayable SQL features remain refused until their pin/reset behavior exists, so they cannot silently enter the shared path.

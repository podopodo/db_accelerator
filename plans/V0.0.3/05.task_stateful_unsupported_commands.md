# Task 05: stateful and unsupported commands

Status: `TODO`

Assignee: `unassigned`

Depends on: `01.task_logical_session_model.md`

## Goal

Handle difficult commands conservatively and visibly.

## Work

- [ ] Build command and SQL feature support table.
- [ ] Pin temporary tables, locks, user variables, streaming cursors, and non-replayable settings.
- [x] Define behavior for multi-statements, stored routines, local infile, replication commands, admin commands, and unknown packets.
- [x] Add stable unsupported-feature errors where forwarding is unsafe.
- [ ] Record pin and rejection reason metrics.
- [x] Publish limitations without hiding them behind generic errors.

## Tests

- [ ] Each unsupported feature has explicit test.
- [ ] Unknown packet cannot desynchronize session.
- [ ] Pin is released only after safe reset.

## Done when

- [x] No unsupported feature silently enters multiplexed path.
- [ ] Support table matches code behavior.
- [ ] Evidence recorded and status set to `DONE`.

## Evidence

Commit/PR:

Support table: README pooled-mode safety boundary plus classifier tests. Full command-by-command table and rejection metrics remain open.
